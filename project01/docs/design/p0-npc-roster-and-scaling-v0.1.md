# P0 NPC 敌人名册与难度缩放 v0.1

> 状态：当前首版敌人主源。仅定义三种原生 NPC 与“英雄 NPC 队长”规则；其余历史敌人、功能怪与 Boss 占位均不进入 P0。

## 范围

P0 的原生 NPC 仅有以下三种：

| 单位 | 身份 | 作用 | 是否可作为队长 |
| --- | --- | --- | --- |
| 步卒 | 近战小兵 | 基础数量与正面接战压力 | 否 |
| 弓手 | 远程小兵 | 后排持续消耗与前排保护教学 | 否 |
| 力士 | 近战队长 | 厚血、慢攻、重击的前排检查点 | 是 |

- 力士是敌方 9 宫格中某一路的**队长单位**，不是小兵。力士死亡后，其所属 NPC 小兵仍按既定规则继续战斗。
- 不制作祭酒、渠帅、盾兵、破甲兵、术士、功能怪或独立 Boss 等原生 NPC。
- 当波次需要更丰富的敌方队长时，使用已有三国英雄的 NPC 变体，而不是新增第四种 NPC。

## 难度 1 基础面板

| 单位 | 最大生命 | 物理攻击 | 物理防御 | 攻击间隔(秒) | 普攻距离 | 击杀经验 | P0 技能/行为 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 步卒 | 850 | 72 | 6 | 1.65 | 300 | 10 | 仅近战普攻。 |
| 弓手 | 650 | 88 | 3 | 1.75 | 600 | 18 | 仅远程普攻。 |
| 力士 | 3,200 | 150 | 26 | 1.85 | 300 | 35 | 普攻为主；可配 1 个“重击”队长技能，180% 攻击、8 秒 CD、0.5 秒前摇。 |

数值意图：步卒与弓手不是独立精英，必须依靠数量和队长保护形成压力；力士的基础防御约为 20.6% 物理减伤，是首轮“厚前排”锚点，但仍可被两个标准英雄组合击破。

## 英雄 NPC 队长

已有三国英雄允许作为敌方某个 9 宫格的队长，规则如下：

```text
EnemyHeroLeaderConfig
= hero_id
+ npc_variant_id
+ npc_level
+ npc_star_level
+ soldier_template_id
+ soldier_count
```

- `hero_id` 复用现有英雄基础面板、属性成长、已设计技能与表现资产；不复制出一套独立英雄定义。
- `npc_variant_id` 承载敌对阵营专属覆写，例如敌方初始站位、小兵模板和小兵数量；它不提供额外属性倍率，也不用于削减技能数量。
- `npc_level` 为波次固定等级，取 1 至 6；不在战斗中获取经验。
- `npc_star_level` 为波次固定星级，取 1 至 6；敌方英雄自动携带所有 `unlock_star_level <= npc_star_level` 的技能。技能数不设 3 个上限，6 星敌方英雄就是满技能敌方英雄。
- 敌方星级与敌方等级独立配置：等级决定基础属性成长，星级决定技能解锁与小兵上限。敌方英雄的小兵上限与我方相同，为 `2 / 3 / 4 / 5 / 6 / 8`；波次可配置低于上限的实际数量，但不得超限。两者均不在战斗中变化。
- 敌方英雄死亡后，其小兵继续作战；这与玩家英雄死亡后小兵溃败的规则不同。

首轮敌方英雄投放建议：难度 1-2 使用 1 级、1-2 星；难度 3-4 使用 2 级、3-4 星；难度 5-6 使用 3 级、4-5 星；难度 7-8 使用 4 级、5 星；难度 9-10 可使用 5-6 级、6 星满技能英雄。满技能英雄每波最多 1 名，并在波次预览中展示星级与关键技能，保证难度可预判。

## 关卡难度属性系数

敌方模板只保存难度 1 的基础属性。关卡难度的**可调属性池**固定为：最大生命、物理攻击、物理防御、攻击速度、自然回血；除这五项外，不得以难度名义直接提高其他属性，新增属性必须另行评审。实际生成时，由关卡难度对指定属性施加可配置乘法系数：

```text
队长等级基础属性 = 基础属性 + (npc_level - 1) × 属性成长
最终 NPC 属性 = round(队长等级基础属性 × 关卡难度属性系数)
```

- 步卒、弓手没有等级成长，直接从基础属性进入难度系数。
- 力士默认 `npc_level = 1`，可由波次覆写。
- 未配置的属性系数固定为 `1.0`；首轮只启用最大生命、物理攻击、物理防御三项。
- 攻击速度与自然回血属于已批准的后续调节池，但首轮系数固定为 `1.0`。若三项成长不足以解决高难度节奏问题，再以单独方案讨论启用范围、上限与关卡档位。
- 攻击范围、命中、闪避、暴击、控制时长和技能 CD 不随难度自动缩放，保证敌人可读性。
- 技能伤害通过物理攻击变化自然增长，不为同一难度额外乘第二次伤害系数。

| 难度 | 最大生命系数 | 物理攻击系数 | 物理防御系数 | 设计定位 |
| ---: | ---: | ---: | ---: | --- |
| 1 | 1.00 | 1.00 | 1.00 | 教学基准。 |
| 2 | 1.12 | 1.06 | 1.01 | 认识混合兵种。 |
| 3 | 1.26 | 1.12 | 1.03 | 首次前排压力。 |
| 4 | 1.42 | 1.18 | 1.05 | 基础组合检查。 |
| 5 | 1.60 | 1.25 | 1.08 | 关卡解锁门槛。 |
| 6 | 1.82 | 1.33 | 1.10 | 复刷起点。 |
| 7 | 2.08 | 1.42 | 1.13 | 构筑开始分化。 |
| 8 | 2.38 | 1.52 | 1.17 | 完整流派压力。 |
| 9 | 2.72 | 1.63 | 1.21 | 高压波次。 |
| 10 | 3.10 | 1.75 | 1.26 | 关卡完全挑战。 |

## 落表字段

| 表 | 新增/关键字段 |
| --- | --- |
| `EnemyUnitConfig` | `enemy_unit_id`、`unit_kind`、难度 1 基础属性、攻击间隔、普攻距离、`xp_reward`、是否队长。 |
| `EnemyHeroLeaderConfig` | `npc_variant_id`、`hero_id`、`npc_level`、`npc_star_level`、小兵模板/数量。 |
| `DifficultyStatMultiplierConfig` | `stage_id`、`difficulty_id`、`hp_multiplier`、`physical_attack_multiplier`、`physical_defense_multiplier`、`attack_speed_multiplier`、`natural_regen_multiplier`；后两项 P0 固定为 `1.0`。 |
| `WaveConfig` | 每路队长 ID、队长变体/等级、步卒/弓手数量、难度系数档位、`wave_clear_xp_bonus`（默认 0，仅特殊额外奖励）。 |

## 后续校验

1. 用关卡 1 难度 1 的“力士队长 + 步卒 / 弓手”组合验证 2 人默认阵容的 15 至 25 秒波次目标。
2. 以难度系数 5、10 分别验证 2 人与 4 人阵容，不通过额外提高攻速或控制时长制造难度。
3. 敌方英雄首次投放时，逐个验证星级解锁出的完整技能循环；6 星满技能英雄必须在波次预览明确告知，且每波不超过 1 名。
