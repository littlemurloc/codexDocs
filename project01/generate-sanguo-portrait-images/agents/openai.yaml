interface:
  display_name: "Three Kingdoms Character Assets"
  short_description: "Create approval-gated half-body art and avatars from 45-degree model references"
  default_prompt: "Use $generate-sanguo-portrait-images to create a character half-body portrait from this 45-degree model reference."

policy:
  allow_implicit_invocation: true
